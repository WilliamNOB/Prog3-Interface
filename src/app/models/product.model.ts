export class Product {
  id?: number; //EL ? PARA INDICAR QUE EL CAMPO NO ES OBLIGATORIO
  name: string;
  description: string;
  price: number;
  cuantity: number;
  lot_id: number;
  customer_id: number;
}
